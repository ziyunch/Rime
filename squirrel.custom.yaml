patch:
#  us_keyboard_layout: true                 # 键盘选项：应用美式键盘布局
# 状态通知，适当，也可设为全开（always）全关（never）
#  show_notifications_when: appropriate
#
  style/color_scheme: clean_white                 # 选择配色方案
  style/horizontal: true                   # 候选窗横向显示
  style/inline_preedit: false              # 关闭内嵌编码，这样就可以显示首行的拼音
  style/corner_radius: 10                  # 窗口圆角半径
  style/border_height: 0                   # 窗口边界高度，大于圆角半径才有效果
  style/border_width: 0                    # 窗口边界宽度，大于圆角半径才有效果
  style/line_spacing: 1                    # 候选词的行间距
  style/spacing: 5                         # 在非内嵌编码模式下，预编辑和候选词之间的间距
  style/font_face: "HiraginoSansGB-W3,HanaMinB"   # 字体名称
  style/font_point: 21                     # 字号

# 注：预设的配色方案及代码（指定为 style/color_scheme ）
#   碧水 - aqua
#   青天 - azure
#   明月 - luna
#   墨池 - ink
#   孤寺 - lost_temple
#   暗堂 - dark_temple
#   星际我争霸 - starcraft
#   谷歌 - google
#   晒经石 - solarized_rock
#   简约白 - clean_white

# 自定義 Mac 應用程序的初始轉換狀態，首先查看應用的 Info.plist 文件得到 該應用的 Bundle Identifier，通常是形如 com.apple.Xcode 的字符串

app_options/com.apple.TextEdit:
    ascii_mode: true
app_options/com.apple.finder:
    ascii_mode: true
app_options/com.apple.Xcode:
    ascii_mode: true
app_options/org.gnu.Emacs:
    ascii_mode: true
app_options/com.filemaker.client.advanced12:
    ascii_mode: true
app_options/com.mathworks.matlab:
    ascii_mode: true
app_options/com.microsoft.Excel:
    ascii_mode: true
app_options/com.jetbrains.pycharm:
    ascii_mode: true
app_options/org.rstudio.Rstudio:
    ascii_mode: true
app_options/com.github.atom:
    ascii_mode: true
app_options/com.tencent.xinWeChat: {}
